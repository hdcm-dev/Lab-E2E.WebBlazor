<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MovilidadUrbana.MAUI.Presentation.Localidades"
             x:Class="MovilidadUrbana.MAUI.Pages.LocalidadEditorPage"
             x:DataType="vm:LocalidadEditorViewModel"
             BackgroundColor="{StaticResource Lienzo}"
             Title="{Binding Title}"
             Shell.TabBarIsVisible="False">
    <Grid RowDefinitions="*,Auto">
        <ScrollView>
            <VerticalStackLayout Padding="16" Spacing="20">

                <Border Style="{StaticResource Banda}" IsVisible="{Binding Notice, Converter={StaticResource TextoPresente}}">
                    <Label Text="{Binding Notice}" TextColor="{StaticResource TextoError}" AutomationId="aviso" />
                </Border>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Nombre" Style="{StaticResource Rotulo}" />
                    <Border Style="{StaticResource Campo}"><Border.Triggers><DataTrigger TargetType="Border" Binding="{Binding NombreError, Converter={StaticResource TextoPresente}}" Value="True"><Setter Property="Stroke" Value="{StaticResource TextoError}" /><Setter Property="StrokeThickness" Value="2" /></DataTrigger></Border.Triggers>
                        <Entry x:Name="NombreEntry" Text="{Binding Nombre}" Placeholder="Ej.: Goya" AutomationId="nombre"
                               Keyboard="Text" ReturnType="Next" MaxLength="80" />
                    </Border>
                    <Label Text="{Binding NombreError}" Style="{StaticResource Error}" IsVisible="{Binding NombreError, Converter={StaticResource TextoPresente}}" />
                    <Label Text="{Binding RequisitoDelNombre}" Style="{StaticResource Ayuda}" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Provincia" Style="{StaticResource Rotulo}" />
                    <Border Style="{StaticResource Campo}"><Border.Triggers><DataTrigger TargetType="Border" Binding="{Binding ProvinciaError, Converter={StaticResource TextoPresente}}" Value="True"><Setter Property="Stroke" Value="{StaticResource TextoError}" /><Setter Property="StrokeThickness" Value="2" /></DataTrigger></Border.Triggers>
                        <Grid ColumnDefinitions="*,Auto"><Picker ItemsSource="{Binding Provincias}" SelectedItem="{Binding Provincia}" Title="Elegir provincia" AutomationId="provincia" /><Label Grid.Column="1" Style="{StaticResource Desplegable}" /></Grid>
                    </Border>
                    <Label Text="{Binding ProvinciaError}" Style="{StaticResource Error}" IsVisible="{Binding ProvinciaError, Converter={StaticResource TextoPresente}}" />
                    <Label Text="{Binding RequisitoDeLaProvincia}" Style="{StaticResource Ayuda}" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Código postal" Style="{StaticResource Rotulo}" />
                    <Border Style="{StaticResource Campo}"><Border.Triggers><DataTrigger TargetType="Border" Binding="{Binding CodigoPostalError, Converter={StaticResource TextoPresente}}" Value="True"><Setter Property="Stroke" Value="{StaticResource TextoError}" /><Setter Property="StrokeThickness" Value="2" /></DataTrigger></Border.Triggers>
                        <Entry Text="{Binding CodigoPostal}" Placeholder="Ej.: 3450" AutomationId="codigo-postal"
                               Keyboard="Numeric" ReturnType="Next" MaxLength="8" />
                    </Border>
                    <Label Text="{Binding CodigoPostalError}" Style="{StaticResource Error}" IsVisible="{Binding CodigoPostalError, Converter={StaticResource TextoPresente}}" />
                    <Label Text="{Binding RequisitoDelCodigoPostal}" Style="{StaticResource Ayuda}" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Habitantes" Style="{StaticResource Rotulo}" />
                    <Border Style="{StaticResource Campo}"><Border.Triggers><DataTrigger TargetType="Border" Binding="{Binding HabitantesError, Converter={StaticResource TextoPresente}}" Value="True"><Setter Property="Stroke" Value="{StaticResource TextoError}" /><Setter Property="StrokeThickness" Value="2" /></DataTrigger></Border.Triggers>
                        <Entry Text="{Binding Habitantes}" Placeholder="Ej.: 90000" AutomationId="habitantes"
                               Keyboard="Numeric" ReturnType="Done" ReturnCommand="{Binding SaveCommand}" MaxLength="9" />
                    </Border>
                    <Label Text="{Binding HabitantesError}" Style="{StaticResource Error}" IsVisible="{Binding HabitantesError, Converter={StaticResource TextoPresente}}" />
                    <Label Text="{Binding RequisitoDeLosHabitantes}" Style="{StaticResource Ayuda}" />
                </VerticalStackLayout>

                <Button Text="Eliminar localidad" Style="{StaticResource BotonPeligro}" IsVisible="{Binding IsEdit}"
                        Command="{Binding DeleteCommand}" AutomationId="eliminar" HorizontalOptions="Start" Padding="0" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- La acción de guardar queda fija abajo: no se pierde detrás del teclado ni al desplazarse. -->
        <Border x:Name="BottomBar" Grid.Row="1" BackgroundColor="{StaticResource Superficie}" StrokeThickness="0" Padding="16,12">
            <Button Text="Guardar" Style="{StaticResource BotonPrimario}" Command="{Binding SaveCommand}" AutomationId="guardar" />
        </Border>
    </Grid>
</ContentPage>
